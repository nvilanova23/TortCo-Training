# Tort Co Encryption Training

Upload every item in this folder to the root of one GitHub repository.

Cloudflare Workers build settings:

- Root directory: /
- Build command: leave blank
- Deploy command: npx wrangler deploy

The wrangler.jsonc file publishes the public folder as static assets.
